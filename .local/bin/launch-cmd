#!/usr/bin/env bash

cache="${XDG_CACHE_HOME:-$HOME/.cache}/fuzzel-launch-cmd"
cmd=$(compgen -c | sort -u | fuzzel --dmenu --cache "$cache" --prompt "run: ")
[ -n "$cmd" ] && hyprctl dispatch exec -- "$cmd"
